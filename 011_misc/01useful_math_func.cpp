#include <iostream>
#include <cmath>
using namespace std;
// useful math related fuctions

int main()
{
    double w = 3.94;
    double x = 5;
    double y = 4;
    double z;

    // z = std::max(x, y); // maximum
    // z = std::min(x, y); // minimum
    // z = pow(2, 4); // power
    // z = sqrt(16); // square root
    // z = abs(-3); // absolute value - say mod

    // z = round(w); // round off
    // z = ceil(w); // round up
    // z = floor(w); // round down

    std::cout << z << endl;

    return 0;
}